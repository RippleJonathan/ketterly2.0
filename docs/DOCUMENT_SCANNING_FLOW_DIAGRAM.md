# Document Scanning Feature - Visual Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                     DOCUMENT SCANNING FLOW                          │
└─────────────────────────────────────────────────────────────────────┘

USER ACTION                    SYSTEM RESPONSE                   STATE
═══════════════════════════════════════════════════════════════════════

┌─────────────┐
│ User clicks │
│ "Scan       │──────▶ Open ScanDocumentDialog
│ Document"   │        Request camera permissions
└─────────────┘                    │
                                   ▼
                        ┌──────────────────────┐
                        │ STEP 1: CAMERA VIEW  │
                        └──────────────────────┘
                                   │
            ┌──────────────────────┼──────────────────────┐
            ▼                      ▼                      ▼
    ┌──────────────┐      ┌──────────────┐      ┌──────────────┐
    │ getUserMedia │      │ Edge         │      │ Visual       │
    │ API Call     │      │ Detection    │      │ Overlay      │
    │              │      │ (every 500ms)│      │ Renders      │
    └──────────────┘      └──────────────┘      └──────────────┘
            │                      │                      │
            ▼                      ▼                      ▼
    ┌──────────────┐      ┌──────────────┐      ┌──────────────┐
    │ Video Stream │      │ autoDetect   │      │ Green Edge   │
    │ Displayed    │──────│ Document()   │──────│ Corners      │
    └──────────────┘      └──────────────┘      └──────────────┘
                                   │
                                   │ User positioned document
                                   │ Edges detected
                                   ▼
┌─────────────┐           ┌──────────────┐
│ User clicks │           │ Capture      │
│ "Capture    │──────────▶│ Video Frame  │
│ Page"       │           └──────────────┘
└─────────────┘                    │
                                   ▼
                        ┌──────────────────────┐
                        │ Image Processing     │
                        │ - Perspective        │
                        │   Transform          │
                        │ - Enhancement        │
                        │ - Compression        │
                        └──────────────────────┘
                                   │
                                   ▼
                        ┌──────────────────────┐
                        │ STEP 2: PAGE PREVIEW │
                        └──────────────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        ▼                          ▼                          ▼
┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│ "Retake"     │          │ "Add Page"   │          │ "Done"       │
│ Delete page  │          │ Return to    │          │ Proceed to   │
│ Go to camera │          │ camera view  │          │ review       │
└──────────────┘          └──────────────┘          └──────────────┘
        │                          │                          │
        └──────────────────────────┴──────────────────────────┘
                                   │
                                   ▼
                        ┌──────────────────────┐
                        │ STEP 3: ALL PAGES    │
                        │ REVIEW               │
                        └──────────────────────┘
                                   │
        ┌──────────────────────────┼──────────────────────────┐
        ▼                          ▼                          ▼
┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│ View all     │          │ Delete page  │          │ Enter title  │
│ pages        │          │ (optional)   │          │ (required)   │
└──────────────┘          └──────────────┘          └──────────────┘
                                                             │
                                                             ▼
┌─────────────┐                                   ┌──────────────┐
│ User clicks │                                   │ Title        │
│ "Save as    │──────────────────────────────────▶│ validated    │
│ PDF"        │                                   └──────────────┘
└─────────────┘                                             │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Compress images      │
                                            │ for upload           │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ POST                 │
                                            │ /api/documents/scan  │
                                            └──────────────────────┘
                                                            │
═══════════════════════════════════════════════════════════════════════
                        SERVER-SIDE PROCESSING
═══════════════════════════════════════════════════════════════════════
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Authenticate User    │
                                            │ Get company_id       │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Validate Lead        │
                                            │ Ownership            │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Generate PDF         │
                                            │ (jsPDF)              │
                                            │ - Add images         │
                                            │ - Add page numbers   │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Upload to Supabase   │
                                            │ Storage              │
                                            │ (lead_documents)     │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Create Document      │
                                            │ Record in DB         │
                                            └──────────────────────┘
                                                            │
═══════════════════════════════════════════════════════════════════════
                        CLIENT-SIDE COMPLETION
═══════════════════════════════════════════════════════════════════════
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Success Response     │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Show Success Toast   │
                                            │ "Document saved!"    │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ Close Dialog         │
                                            │ Trigger onSuccess()  │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ React Query          │
                                            │ Auto-Refreshes       │
                                            │ Documents List       │
                                            └──────────────────────┘
                                                            │
                                                            ▼
                                            ┌──────────────────────┐
                                            │ 🎉 COMPLETE!         │
                                            │ Document visible     │
                                            │ in Files tab         │
                                            └──────────────────────┘
```

## Component Hierarchy

```
FilesTab
  └── ScanDocumentDialog (when open)
      ├── DialogContent
      │   ├── DialogHeader
      │   │   ├── DialogTitle
      │   │   └── DialogDescription
      │   │
      │   ├── Camera View (step === 'camera')
      │   │   ├── Webcam (hidden, video stream source)
      │   │   ├── Canvas (visible, with edge overlay)
      │   │   ├── Edge Detection Indicator
      │   │   ├── Page Count Badge
      │   │   └── Capture Button
      │   │
      │   ├── Single Page Preview (step === 'preview')
      │   │   ├── Image Preview
      │   │   └── Action Buttons
      │   │       ├── Retake
      │   │       ├── Add Page
      │   │       └── Done
      │   │
      │   └── All Pages Review (step === 'all-pages')
      │       ├── Pages List
      │       │   └── Page Card (for each page)
      │       │       ├── Badge (page number)
      │       │       ├── Thumbnail
      │       │       └── Delete Button
      │       ├── Title Input
      │       └── Action Buttons
      │           ├── Add Page
      │           └── Save as PDF
      │
      └── DialogFooter
          └── Cancel Button
```

## Data Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                       DATA TRANSFORMATION                        │
└──────────────────────────────────────────────────────────────────┘

Video Frame (HTMLVideoElement)
        │
        ▼
Canvas (HTMLCanvasElement) ───▶ ImageData
        │                            │
        │                            ▼
        │                    Edge Detection
        │                            │
        │                            ▼
        │                    Detected Corners
        │                    { topLeft, topRight,
        │                      bottomRight, bottomLeft }
        │                            │
        ▼                            ▼
Perspective Transform ◀──────────────┘
(applyPerspectiveTransform)
        │
        ▼
Straightened Canvas
        │
        ▼
Enhancement (contrast)
        │
        ▼
Base64 JPEG (compressed)
        │
        ▼
ScanPage Object
{ id, imageData, corners, timestamp }
        │
        ▼
Pages Array
[ page1, page2, page3, ... ]
        │
        ▼
API Request Body
{ leadId, title, pages }
        │
        ▼
PDF Buffer (jsPDF)
        │
        ▼
Supabase Storage File
        │
        ▼
Document Record in Database
{
  id, company_id, lead_id,
  document_type, title,
  file_url, file_name, file_size,
  mime_type, uploaded_by, ...
}
```

## State Management

```
┌──────────────────────────────────────────────────────────────────┐
│                     COMPONENT STATE                              │
└──────────────────────────────────────────────────────────────────┘

step: 'camera' | 'preview' | 'all-pages'
  └─▶ Controls which UI is displayed

pages: ScanPage[]
  └─▶ Stores all captured pages

currentCorners: DetectedCorners | null
  └─▶ Live edge detection results

isDetecting: boolean
  └─▶ Whether edge detection is running

isProcessing: boolean
  └─▶ Whether image is being processed

isSaving: boolean
  └─▶ Whether PDF is being saved

documentTitle: string
  └─▶ User-entered title for document

cameraError: string | null
  └─▶ Error message for camera issues

draggedCorner: keyof DetectedCorners | null
  └─▶ Which corner is being dragged (for manual adjustment)
```

## API Contract

```
┌──────────────────────────────────────────────────────────────────┐
│                  API REQUEST/RESPONSE                            │
└──────────────────────────────────────────────────────────────────┘

POST /api/documents/scan

REQUEST HEADERS:
  Content-Type: application/json
  Cookie: sb-access-token (Supabase auth)

REQUEST BODY:
{
  "leadId": "uuid-string",
  "title": "Scanned Document Title",
  "pages": [
    {
      "id": "page_1234567890_abc123",
      "imageData": "data:image/jpeg;base64,/9j/4AAQ...",
      "corners": {
        "topLeft": { "x": 100, "y": 100 },
        "topRight": { "x": 500, "y": 100 },
        "bottomRight": { "x": 500, "y": 700 },
        "bottomLeft": { "x": 100, "y": 700 }
      },
      "timestamp": 1702314567890
    },
    // ... more pages
  ]
}

RESPONSE (Success - 200):
{
  "success": true,
  "document": {
    "id": "doc-uuid",
    "company_id": "company-uuid",
    "lead_id": "lead-uuid",
    "title": "Scanned Document Title",
    "document_type": "other",
    "file_url": "company-id/lead-id/scan_1234567890.pdf",
    "file_name": "scan_1234567890.pdf",
    "file_size": 245678,
    "mime_type": "application/pdf",
    "uploaded_by": "user-uuid",
    "created_at": "2024-12-11T10:30:00Z",
    // ... other fields
  },
  "message": "Document scanned and saved successfully"
}

RESPONSE (Error - 4xx/5xx):
{
  "error": "Error message describing what went wrong"
}
```

## Security Model

```
┌──────────────────────────────────────────────────────────────────┐
│                   SECURITY BOUNDARIES                            │
└──────────────────────────────────────────────────────────────────┘

CLIENT SIDE:
  └─▶ User Authentication (Supabase Auth)
  └─▶ Company Context (useCurrentCompany hook)
  └─▶ Lead Access (must be from same company)

API ROUTE:
  └─▶ Verify auth token (supabase.auth.getUser())
  └─▶ Get user's company_id from users table
  └─▶ Verify lead belongs to company
      ├─▶ SELECT * FROM leads
      │   WHERE id = ? AND company_id = ?
      └─▶ Reject if not found (404)

SUPABASE STORAGE:
  └─▶ RLS Policy: Users can only access their company's files
  └─▶ Path: company_id/lead_id/filename
  └─▶ Bucket: lead_documents (private)

DATABASE:
  └─▶ RLS Policy: SELECT/INSERT filtered by company_id
  └─▶ documents table includes company_id foreign key
  └─▶ ON DELETE CASCADE for cleanup
```

---

**This diagram provides a complete visual understanding of how the document scanning feature works from start to finish.**
